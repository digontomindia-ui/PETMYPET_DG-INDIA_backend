import { Router } from 'express';

export const apiRouter = Router();

// Module routers are mounted here incrementally as each module is implemented,
// so the app remains buildable and runnable at the end of every phase.
